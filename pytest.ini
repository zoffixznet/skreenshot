[pytest]
testpaths = tests
pythonpath = src
addopts = -p no:cacheprovider
markers =
    e2e: end-to-end tests that need Xvfb, xdotool, xclip and ImageMagick
# Unit tests never touch the live display; e2e tests spawn their own Xvfb.
